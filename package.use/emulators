# no ebuild provides these package, managed manually outside portage in /opt/rpcs3
games-emulation/rpcs3 alsa pulseaudio joystick llvm vulkan

# change to qt5 once its ready (mostly the config dialogs are non-existent)
games-emulation/dolphin alsa pulseaudio evdev ffmpeg openal pch -qt5 sdl wxwidgets

# for yuzu emulator to avoid constant recompiles
dev-util/unicorn unicorn_targets_aarch64 unicorn_targets_arm unicorn_targets_x86
