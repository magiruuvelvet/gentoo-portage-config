# install 059-r7
>=sys-kernel/dracut-060

# fontconfig 2.17 seems to have regressions and selects wrong fonts in my multi-locale environment (multiple language scripts).
# blacklist for now until I've figured out what is going on.
# the wrong font selection causes extremely poor visuals in some important places.
>=media-libs/fontconfig-2.17
